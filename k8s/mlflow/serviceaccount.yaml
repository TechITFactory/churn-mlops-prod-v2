apiVersion: v1
kind: ServiceAccount
metadata:
  name: mlflow-sa
  annotations:
    # Add your IRSA annotation here if using AWS IAM roles for service accounts
    # eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/your-mlflow-role
    {}
